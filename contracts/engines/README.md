# Engine contracts

`contracts/engine_registry.json` materializes the 112-row catalog only. It does
not constitute 112 implemented engine contracts. All engine runtime statuses
remain `NOT_IMPLEMENTED`.

Per-engine contracts must later contain the full V3 fields and eight acceptance
test classes before R1 can be considered.

